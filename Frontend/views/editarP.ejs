<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Editar Perfil — Vapor</title>
  <link rel="stylesheet" href="../CSS/styles.css" />
</head>
<body>

  <header class="header">
    <div class="logo">
      <span class="logo-mark">VAPOR</span>
    </div>
    <a href="./perfil.ejs" class="btn profile-btn">← Volver al Perfil</a>
  </header>

  <main class="main">
    <section class="card">
      <h1 class="card-title">Cambiar Contraseña</h1>
      <p class="card-subtitle">Actualiza tu contraseña de acceso</p>

      <form class="form" id="cambiarPasswordForm">
        <div class="form-group">
          <label for="currentPassword">Contraseña actual</label>
          <input 
            type="password" 
            id="currentPassword" 
            placeholder="Ingresa tu contraseña actual" 
            required
            maxlength="10"
          />
          <span class="error-msg" id="currentPasswordError"></span>
        </div>

        <div class="form-group">
          <label for="newPassword">Nueva contraseña</label>
          <input 
            type="password" 
            id="newPassword" 
            placeholder="8-10 caracteres" 
            required
            maxlength="10"
          />
          <span class="error-msg" id="newPasswordError"></span>
          <small class="form-hint">Mínimo 8, máximo 10 caracteres</small>
        </div>

        <div class="form-group">
          <label for="confirmPassword">Confirmar nueva contraseña</label>
          <input 
            type="password" 
            id="confirmPassword" 
            placeholder="Repite la nueva contraseña" 
            required
            maxlength="10"
          />
          <span class="error-msg" id="confirmPasswordError"></span>
        </div>

        <div class="form-footer">
          <button type="submit" class="btn btn-primary">Actualizar Contraseña</button>
          <a href="./perfil.ejs" class="btn">Cancelar</a>
        </div>
      </form>
    </section>
  </main>

  <footer class="footer">
    <p class="muted">© 2025 GRAM — Vapor</p>
  </footer>

  <script src="../JS/editarP.js"></script>
</body>
</html>